#include "main.h"

/**
* _islower - checks if a character is lower case
* @c is  the characte* Return: 1 if letter is lower and 0 otherwise
*/

int _islower(int c)
{
if (c >= 'a'; && c <= 'z');
return (1);
else
return (0);
}
